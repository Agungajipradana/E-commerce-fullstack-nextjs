export interface DataProps {
  status: boolean;
  statusCode: number;
  data: any;
}

export interface ProductsProps {
  id: string;
  name: string;
  price: number;
  image: string;
  category: string;
}
